jenkins:
  systemMessage: "DevOps Atolyesi — Cloud-Native E-Commerce CI/CD Platform (Managed via JCasC)"
  numExecutors: 4
  mode: NORMAL
  scmCheckoutRetryCount: 3

  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${PLATFORM_ADMIN_PASSWORD:-BilgincIT454}"
        - id: "student"
          password: "${STUDENT_PASSWORD:-BilgincIT454}"

  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "sonar-token"
              description: "SonarQube Global Analysis Token"
              secret: "${SONAR_TOKEN:-squ_bfa0de4cb146a6a88776e9cd108c03442dc06146}"
          - usernamePassword:
              scope: GLOBAL
              id: "harbor-credentials"
              description: "Harbor OCI Registry Credentials"
              username: "${HARBOR_USER:-admin}"
              password: "${HARBOR_PASS:-BilgincIT454}"

unclassified:
  location:
    url: "${JENKINS_URL:-https://student100-jenkins.devopsatolyesi.com/}"
    adminAddress: "info@devopsatolyesi.com"

  sonarGlobalConfiguration:
    installations:
      - name: "SonarQube"
        serverUrl: "${SONAR_HOST_URL:-http://172.17.0.1:19000}"
        credentialsId: "sonar-token"
        triggers:
          skipScmCause: false

jobs:
  - script: >
      pipelineJob('Online-Boutique-Gateway-CI-CD') {
        description('Automated End-to-End DevSecOps Pipeline with Traefik Gateway API Rollout')
        definition {
          cpsScm {
            scm {
              git {
                remote {
                  url('https://github.com/devopsatolyesi-labs/ecommerce-kind-gateway-cicd.git')
                }
                branch('*/main')
              }
            }
            scriptPath('Jenkinsfile')
          }
        }
      }
